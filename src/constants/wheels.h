#ifndef WHEELS_CONSTANTS_H
#define WHEELS_CONSTANTS_H

#include <Arduino.h>

// Wheel constants
const float WHEEL_CIRCUM = 0.21;
const byte WHEEL_STEPS = 20;
const unsigned int WHEEL_CALC_INTERVAL = 200;

#endif
